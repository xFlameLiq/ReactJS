# ReactJS

## Clonar el respositorio  
`$ git clone https://github.com/xFlameLiq/ReactJS.git`
`$ cd ReactJS/`

## Frontend
`$ cd reactjs/`
`$ npm install`
`$ npm run dev`

## Backend
`$ cd backend/`
`$ npm install`
`$ npm run start`
